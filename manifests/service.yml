apiVersion: v1
kind: Service
metadata:
    name: "k8cluster-c04e"
    labels:
        app: "k8cluster-c04e"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "k8cluster-c04e"